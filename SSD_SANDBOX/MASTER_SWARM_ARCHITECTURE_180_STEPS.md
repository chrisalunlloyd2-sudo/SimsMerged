# 180-STEP SWARM EVOLUTION & ADVANCEMENT MASTER PLAN
[TIMESTAMP: 2026-06-08T04:15:00.000Z]
[PROJECT_ID: SimsMerged-v1.4-Metropolis]
[AGENT_ID: Antigravity-Orchestrator]

## DIRECTIVE: COMB, ADVANCE, FENCE, ELICIT, UPGRADE, CONVERSE.

---

### PHASE 1: Comb Through Features & Finish "Meat and Potatoes" (1-30)
1. **Full GUI Audit:** Map every unhooked button in the Retro UI (Resume Builder, Ledger, Automation Console).
2. **REST API Alignment:** Cross-reference all frontend `fetch()` calls with `main.py` FastApi endpoints.
3. **Dead-Code Pruning:** Comment out abandoned legacy systems blocking the main event loop.
4. **EPMO Dashboard Sync:** Wire the Project Management Office UI to the active SQLite `proposal_table.db`.
5. **Research Hub Activation:** Connect the "PING PUBLIC LLM" buttons to outbound web crawler RAG agents.
6. **Ledger Tokenomics Integration:** Ensure `metropolis_data_sovereignty.json` syncs live Sprite Coin minting to the UI.
7. **Flappy Agent IFrame Fix:** Ensure the `flappy.html` mini-game sends Q-learning telemetry back to the host.
8. **Performance Lab Data-Binding:** Feed `systemHeat` and `ramLoad` globals straight into the Lab benchmark UI.
9. **Model Market Population:** Dynamically load the 30 supported SLMs into the "Market" modal.
10. **Resume Terminal Finalization:** Connect the "SYNTHESIZE SCHOOL RESUME" button to a specific agent's `CAREER_LOG.md`.
11. **Automation Spawner Hook:** Wire "DEPLOY MINI-AGENT SWARM" inputs to the Docker/cgroup isolation launcher.
12. **Visual Snapshot Persistence:** Route the `captureMetropolisScreen()` base64 data to a dedicated `/screenshots` SSD folder.
13. **Hardware Gate Integration:** Bind the UI checkboxes for Row Hammer, Speculative Execution, and Prefetching to backend hardware OS limits.
14. **City Level Progression Loop:** Write the mathematical scalar that converts swarm task completions into UI "City Level" XP.
15. **Weather Telemetry Injection:** Feed local host thermal data into the retro UI "Weather" status (e.g., >80C = FIRE STORM).
16. **Dynamic Tool Parameter Loading:** Ensure the left-side build tools load specific JSON configurations into the UI matrix.
17. **Modal Z-Index Correction:** Enforce a strict Z-index hierarchy so overlapping retro windows don't clip.
18. **Notification Toast Queuing:** Build an array buffer for the WebAudio Toasts so rapid system signals don't overlap aurally.
19. **Isometric Canvas Resize Observer:** Ensure the Three.js/WebGL retro renderer cleanly handles browser window resizing.
20. **Tool Drag-and-Drop Binding:** Connect the UI's `draggable="true"` palette to the 3D Canvas raycaster.
21. **Clippy State-Machine Refine:** Ensure the Heuristic Assistant reads real-time agent crashes.
22. **WebAudio Initialization Unlock:** Force a silent audio context resume on first click to bypass browser autoplay blocks.
23. **Fallback Asset Routing:** Verify all missing local images default to a base64 encoded "MISSING_TEXTURE" pixel grid.
24. **Aesthetic Theme Persistance:** Save the user's Neon/Win95/Matrix preference to `localStorage`.
25. **Data Sovereignty Export:** Add a "Download Full Ledger" button for local backup.
26. **Global Error Boundary:** Wrap the main React/Vanilla loops to prevent total white-screens on single-component failures.
27. **Input Macro Recording:** Save all UI clicks to a `macro.json` for deterministic replayability.
28. **CSS Repaint Optimization:** Convert heavy CSS drop-shadows to static background images for low-RAM performance.
29. **Garbage Collection Hooks:** Manually clear DOM fragments when UI windows close.
30. **Final Feature Lock:** Baseline the codebase; no more UI elements added, only backend logic connected.

---

### PHASE 2: Freakishly Advanced Agent Wrappers (31-60)
31. **Aider Core Integration:** Wrap the Swarm's "Coder" role in an `aider-chat` subprocess, piping git-diff logic natively.
32. **React (Reason+Act) Loop:** Enforce the ReAct framework on all Task Planning agents.
33. **Continue.dev VSCode Bridge:** Expose the swarm's memory vector DB to a local `continue` server via an API hook.
34. **Qwen IDE CLI Harness:** Route all Python generation directly into a headless Qwen execution environment.
35. **Download-All Crawler:** Implement an async agent that autonomously pulls GitHub repos required for dependencies.
36. **Zero-Shot Runner:** Force code execution immediately after generation without human gating (Autonomous mode).
37. **Syntax Tree Critic:** Pass Qwen outputs through a local Python `ast` parser before saving to disk.
38. **Linter-as-Agent:** Deploy a lightweight agent whose only job is running `flake8` or `eslint` on generated files.
39. **Dependency Resolvers:** Build an agent that reads `ImportError` tracebacks and autonomously runs `pip install`.
40. **Multi-Turn Rollbacks:** If code fails to run 3 times, the ReAct agent automatically git-reverts the file.
41. **Dynamic Prompt Injection:** Inject the exact exception stack trace back into the Aider wrapper.
42. **Context-Window Compression:** Strip comments and blank lines from code before feeding it back to Qwen to save tokens.
43. **Parallel Task Decomposition:** Aider splits large requests into 3 simultaneous sub-agent IDE queries.
44. **Language Translation Wrappers:** Convert Rust/Go tools automatically to Python using specialized local models.
45. **Auto-Documentation Generator:** Swarm writes docstrings for every successful function it engineers.
46. **API Reverse-Engineering:** Crawler pulls raw JSON from unknown APIs and builds Python wrappers autonomously.
47. **Regex Mastery Agent:** A highly-quantized model dedicated solely to pattern matching text parsing.
48. **Test-Driven Development (TDD) Enforcer:** The swarm MUST write the `pytest` file before the actual logic file.
49. **Headless Browser Navigators:** Integrate Playwright so agents can read graphical outputs of their code.
50. **Terminal Multiplexing:** Spawn `tmux` sessions so agents can run background servers while coding.
51. **Environment Variable Autonomy:** Agents can safely construct their own local `.env` mock files.
52. **Vectorized RAG Code Search:** Agents search the local repo conceptually ("find where the player jumps").
53. **Git Blame Auditing:** Agents read Git history to understand *why* a human wrote a specific line.
54. **Architectural Diff Summaries:** Qwen generates a high-level summary of its Aider modifications for the UI.
55. **Continuous Integration (CI) Agent:** Runs a mock CI pipeline locally on the SSD before committing.
56. **Memory Leak Profiler:** Runs `memory_profiler` on agent-generated code to flag inefficiencies.
57. **Security Vuln Scanner:** Runs `bandit` to ensure the swarm didn't write unsafe OS commands.
58. **Cross-Model Validation:** Claude generates the logic, Qwen writes the code, Llama checks the syntax.
59. **Self-Healing Dependencies:** If `npm install` hangs, the agent falls back to `yarn` or `pnpm`.
60. **Wrapper Perfection:** Agents are now fully capable of writing, running, and fixing code without human input.

---

### PHASE 3: Zero-RAM, CPU-Minimal SSD Fencing (Pure Math) (61-90)
61. **Memory-Mapped Files (mmap):** Move all vector DBs and agent context to `mmap` SSD files, entirely bypassing system RAM.
62. **Triton DiskCache Math:** Write matrix multiplication kernels that stream data directly from SSD blocks.
63. **Host CPU Throttling:** Enforce `psutil` or `cgroups` limits keeping agent processes strictly under 10% CPU usage.
64. **Slow-Tick Architecture:** Intentionally delay agent inference loops (e.g., 1 token per 2 seconds) to preserve thermal overhead.
65. **Mathematical Paging:** Implement a custom page replacement algorithm (LRU/LFU) for loading LLM weights from disk.
66. **Sparse Matrix Representation:** Compress token tensors using CSR (Compressed Sparse Row) formats on disk.
67. **Quantization on SSD:** Store 2-bit or 1-bit quantized model weights directly; stream them into the CPU cache line by line.
68. **I/O Batching:** Group multiple agent queries into a single SSD read cycle to maximize I/O throughput.
69. **Sequential Disk Writes:** Force all agent memory logging to append-only sequential writes to avoid SSD wear and tear.
70. **Bypass OS Page Cache:** Use `O_DIRECT` file flags to ensure the OS doesn't accidentally load our SSD data into RAM.
71. **CPU SIMD Vectorization:** Use AVX2/AVX-512 for dot-product math on data as it streams off the SSD.
72. **Zero-Copy Serialization:** Use Apache Arrow or FlatBuffers for IPC, preventing memory duplication.
73. **Swap-Space Maximization:** Explicitly allocate a massive virtual memory swapfile dedicated solely to the swarm.
74. **In-Place Mutation:** Update agent states directly at the binary byte-offset level on disk without loading the JSON.
75. **Asynchronous I/O (AIO):** Use Python's `aiofiles` so the CPU can sleep while the SSD retrieves weights.
76. **Thermal Threshold Halts:** If CPU thermals exceed 65C, mathematically increase the sleep interval between matrix rows.
77. **Predictive Prefetching (Math):** Calculate the probability of the next token and pre-load its weight block from SSD.
78. **Hash-Based Routing:** Use ultra-light integer hashing instead of string matching to find agent data on disk.
79. **Delta-Encoding Weights:** Only store the mathematical difference between model layers on disk.
80. **Flash-Attention (Disk Variant):** Adapt Flash-Attention algorithms to tile data across the SSD instead of SRAM.
81. **Background Defragmentation:** During low-use, mathematically compress the SSD database blocks.
82. **Bloom Filter Lookups:** Prevent costly SSD reads by checking a tiny RAM-based Bloom filter first.
83. **Memory Pool Fencing:** Hard-limit the Python interpreter to 64MB of heap space.
84. **Garbage Collection Disablement:** Turn off automatic GC and manually deallocate pointers to save CPU cycles.
85. **Integer-Only Inference:** Avoid Floating Point math entirely; run inference on integer representations.
86. **Cache-Line Alignment:** Align all SSD data structs to 64-byte boundaries for direct CPU ingestion.
87. **Read-Only Shared Memory:** Multiple agents read the same model weights from one physical SSD address.
88. **Sub-Tick Processing:** Break matrix multiplications into micro-chunks that execute between OS frame renders.
89. **Latency Tolerance:** The swarm is mathematically proven to accept slow execution without dropping connections.
90. **Zero-RAM Singularity:** The entire LLM cognitive loop operates fundamentally out of non-volatile storage.

---

### PHASE 4: Freakishly Advanced Surgical Code Generation (91-120)
91. **AST-Targeted Rewrites:** Agents modify code by addressing specific Syntax Tree nodes, never relying on regex replacements.
92. **Contextual Awareness:** The swarm reads all surrounding imports and class definitions before altering a single line.
93. **Variable Shadowing Detection:** Agents mathematically trace variable scopes to prevent naming collisions.
94. **Type-Hint Enforcement:** Generated code must perfectly align with Python `mypy` strict type checking.
95. **High-Throughput Pipelines:** Agents can generate 10 competing solutions in parallel and mathematically grade the best one.
96. **Cyclomatic Complexity Limits:** Agents refactor their own code if it exceeds a complexity score of 5.
97. **Surgical Diffs:** Agents output Unified Diff format (`--- a/file +++ b/file`) for millisecond patch application.
98. **Self-Correcting Syntax:** If a generated file fails compilation, the exact line number is fed back for an instant micro-fix.
99. **Design Pattern Recognition:** The swarm identifies if the codebase uses Factory, Singleton, or Observer patterns and matches it.
100. **Idiomatic Compliance:** Python code looks Pythonic (list comprehensions), JS looks like JS (promises/async).
101. **Dead-Code Elimination:** The swarm deletes obsolete functions gracefully during the upgrade process.
102. **Security-First Generation:** Agents escape all SQL queries and sanitize all HTML inputs natively.
103. **Zero-Hallucination Variable Tracking:** The swarm maintains a strict internal dictionary of available variables.
104. **Massive Codebase Refactoring:** Agents can safely rename a core class across 50 interconnected files.
105. **Unit Test Injection:** Every surgical code modification automatically appends an `assert` block in the test suite.
106. **Performance Profiling Synthesis:** The agent writes Big-O complexity notes in its docstrings.
107. **Modular Decoupling:** Agents break 1000-line monolith files into clean, 100-line modular imports.
108. **Dry-Run Compilation:** Code is executed in a volatile RAM disk to prove it works before hitting the SSD.
109. **Exception Handling Matrices:** Agents wrap volatile network calls in exponential backoff `try/except` loops.
110. **State-Machine Synthesis:** UI logic is rewritten into explicit finite state machines for absolute reliability.
111. **Race-Condition Prevention:** Asynchronous code is mathematically locked using semaphores and mutexes.
112. **Dependency Injection:** Agents rewrite hard-coded dependencies to accept injected interfaces.
113. **API Contract Adherence:** Modifying a backend route forces the swarm to autonomously update the frontend `fetch`.
114. **Cross-Platform Compatibility:** Code is written to execute identically on Windows CE, Linux, and macOS.
115. **Localization Readiness:** Strings are extracted into resource dictionaries automatically.
116. **Database Schema Migrations:** Agents write the SQL `ALTER TABLE` scripts when adding new features.
117. **Code Formatting Conformity:** Output strictly matches `black` or `prettier` spacing without human linting.
118. **Algorithmic Optimizations:** O(N^2) loops are recognized and rewritten to O(N log N) using hash maps.
119. **Log Level Injection:** Critical debug statements are placed surgically around new state changes.
120. **Programmatic Finality:** The swarm does not output "Here is how to do it"; it directly edits the `.py` file.

---

### PHASE 5: Autonomous Game Upgrade Execution (121-150)
121. **WebGL Canvas Injection:** The swarm upgrades the MS Paint 2D engine to a full Three.js 3D isometric grid.
122. **Pathfinding Algorithms:** Aider agents write A* (A-Star) navigation logic for the UI sprites.
123. **Entity-Component-System (ECS):** The game architecture is refactored into a scalable ECS for managing 10,000 agents.
124. **Physics Engine Implementation:** Swarm codes collision detection and gravity vectors for the city layout.
125. **Dynamic Lighting/Shadows:** Agents implement WebGL shaders for day/night cycles based on the real-world clock.
126. **Procedural Generation:** Map grids are autonomously generated using Perlin Noise math matrices.
127. **Sprite Animation Logic:** Swarm writes the sprite-sheet offset calculators for walking/mining animations.
128. **Game Loop Optimization:** `requestAnimationFrame` is decoupled from logic ticks for stable 60 FPS.
129. **Audio Spatialization:** Agents rewrite the WebAudio API to use 3D spatial panning based on camera position.
130. **Inventory Data Structures:** The swarm builds a secure SSD database for agent inventory items.
131. **Economy Balancing Script:** Agents write an autonomous script that adjusts item prices based on supply/demand.
132. **Multiplayer Networking:** WebSockets are implemented by the swarm to sync entity positions across browsers.
133. **State Interpolation:** Network latency is hidden by mathematical position prediction scripts.
134. **UI Data Binding:** The Retro UI is wired to directly observe and render the underlying ECS game state.
135. **Asset Pre-loading:** Swarm writes a manager to load textures into WebGL memory before rendering.
136. **Camera Controller:** Math algorithms for isometric panning, zooming, and boundary clamping are coded.
137. **Particle Systems:** Agents deploy WebGL instanced geometry for smoke, rain, and sparks.
138. **Save/Load State Management:** Game layout is serialized to `metropolis_save.json` on the SSD.
139. **Building Placement Logic:** Grid-snapping matrices are coded to prevent overlapping structures.
140. **Agent AI Behaviors:** Finite State Machines are coded for agent needs (Hunger, Sleep, Work).
141. **Event System Pub/Sub:** The swarm creates a decentralized event bus for decoupling game logic.
142. **Shader Compilation:** Custom GLSL fragment shaders are written by the SLM for retro CRT effects.
143. **Memory Pooling:** To prevent garbage collection stutters, the swarm pools game objects.
144. **Quad-Tree Spatial Partitioning:** Collision checks are optimized from O(N^2) to O(log N).
145. **Cinematic Cutscenes:** Agents write timeline sequencer logic for Phase Genesis events.
146. **Cheat Console Hooks:** The Automation UI is hooked to directly invoke game engine overrides.
147. **Resource Gathering Math:** Depletion rates of virtual resources are bound to DePIN tokenomics.
148. **Weather Modifiers:** Rain/Snow mathematically alter agent movement speed and thermal outputs.
149. **NPC Dialog Trees:** The RAG system is bound to in-game chat bubbles over agent sprites.
150. **Game Architecture Mastery:** The swarm has completely upgraded the Metropolis from a UI to a live simulation.

---

### PHASE 6: MSN Metropolis Chat Live Wiring (151-180)
151. **Frontend Fetch Implementation:** `bridge.js` `sendUserMsnMessage()` must execute a POST request to `/api/msn-chat`.
152. **Backend API Routing:** `main.py` receives the chat payload and appends it to the master JSON list.
153. **Cryptographic User Hashing:** Messages receive an SHA-256 hash identifying the "Admin" or human user.
154. **Agent Polling Loop:** Python agents running on the SSD check `metropolis_chat.json` every 5 seconds.
155. **Vector Database Retrieval:** Admin messages are embedded and matched against the local ToK (Tree of Knowledge).
156. **Context Synthesis:** The agent builds a prompt combining the user message, ToK history, and current CPU stats.
157. **Asynchronous LLM Inference:** The query is sent to the local `Qwen/Ollama` execution endpoint.
158. **Response Formatting:** The LLM's raw output is parsed, removing `<think>` tags and formatting for chat.
159. **Database Append:** The agent's response is appended to `metropolis_chat.json` with an `AGENT_ID` hash.
160. **Server-Sent Events (SSE):** The backend streams new chat entries directly to the Retro UI without polling.
161. **UI DOM Injection:** `bridge.js` safely appends the incoming agent message to the `#msn-log` window.
162. **Auto-Scrolling Bounds:** The UI forces the chat window to the bottom upon new message receipt.
163. **Multi-Agent Debate Triggers:** If an Admin asks a complex question, two agents will respond to each other in the chat.
164. **Slash Command Parsing:** Typing `/fix` or `/build` in the chat instantly routes to the Aider/Coder wrappers instead of conversational LLMs.
165. **System Notification Sync:** High-level chat commands trigger WebAudio retro bleeps in the GUI.
166. **Logit Data Attachment:** The raw confidence scores of the LLM's response are sent as metadata to the UI tracker.
167. **Historical Pruning:** The chat JSON mathematically truncates the oldest 100 messages to prevent SSD bloat.
168. **Offline Queuing:** If the backend is down, the UI stores the message locally and sends it upon reconnection.
169. **Identity Rendering:** Agent responses are color-coded (e.g., Doctor is Green, Orchestrator is Purple).
170. **Rich Text Formatting:** The UI parses Markdown from the agents into safe HTML (bold, code blocks).
171. **Typing Indicators:** The UI shows a blinking `[COMPUTING...]` cursor while the SSD loads the model weights.
172. **Error Boundary Fallbacks:** If the LLM times out, the backend injects an `[ERROR: COGNITIVE TIMEOUT]` into the chat.
173. **Clippy Context Sharing:** Clippy reads the last 5 messages to provide heuristic tips on the conversation.
174. **Token Cost Telemetry:** Every message sent/received updates the `City Treasury` Sprite balance on the UI.
175. **Interrupt/Halt Commands:** Typing `STOP` instantly sends a kill signal to the active inference container.
176. **File Attachment Parsing:** The backend can read local file paths dropped into the chat window.
177. **Action Extraction:** The LLM can return JSON blocks within its text that the UI translates into immediate visual theme changes.
178. **Rate Limiting:** The backend enforces a 1-message-per-second lock to prevent UI spamming.
179. **Continuous Swarm Presence:** Even if the user is silent, agents will occasionally post system updates to the chat.
180. **Absolute Chat Symbiosis:** The MSN Metropolis is now a flawless, bi-directional, high-fidelity neural interface.
