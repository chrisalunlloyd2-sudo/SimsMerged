# [TIMESTAMP: 2026-06-12T21:10:00.000Z] [PROJECT_ID: SimsMerged-v1.4.2] [AGENT_ID: viper_cli-architectssj4]

# METROPOLIS MASTER TODO LIST (EXTENSIVE)
*An exhaustive, granular breakdown of every systemic task required for Absolute System Supremacy.*

## 📈 ML & PATTERN RECOGNITION
- [ ] Verify `ml_orchestrator.py` correctly deletes old `syslog_*.log` shards to prevent SSD bloating.
- [ ] Enhance the `TfidfVectorizer` in ML Orchestrator with N-gram logic (n=(1,3)) for better contextual pattern extraction.
- [ ] Build the "Advisory Pipeline" to stream ML Orchestrator findings directly into the JavaFX UI Omni-HUD.
- [ ] Run a 24-hour test to confirm the TF-IDF feature extraction mathematically correlates with actual system crashes.

## 🧬 DATA SYPHON & EPMO BUSINESS SCHOOL
- [ ] Write a script to automatically download `aider` and `qwen-ide` dependencies and permanently add them to the Windows PATH.
- [ ] Force the `LeanSixSigmaEPMO` class to aggressively penalize code that does not contain error-handling (try/except blocks).
- [ ] Populate the `never_make_code_twice.duckdb` to ~15GB size by scraping public GitHub repositories using an autonomous web crawler agent.
- [ ] Test one local model (e.g., `smollm:135m`) executing a full Darwinian loop using ONLY ghost code as its context.
- [ ] Validate that variables inside generated scripts are perfectly tethered and linked across files.

## 🌐 AGENTIC GITHUB SUITE
- [x] Build `agentic_github_suite.py` utilizing the `GitPython` library.
- [ ] Program the suite to autonomously perform `git status`, diff analysis via SLM, and execute commit/push loops every 6 hours.
- [ ] Connect the GitHub suite to the "Never Make Code Twice" DB, so the system only commits code that scores >8.5 on the Lean Six Sigma scale.
- [ ] Ensure the Suite creates branch names dynamically based on the SLM performative (e.g., `feature/epmo-syphon-upgrade`).

## 🖥️ JAVA GUI & GRAPHICS
- [ ] Complete the "Data Syphon" visual connection between coding agents and the `Neural_Terminal`.
- [ ] Add tooltips to the `EPMODashboardWindow` explaining exactly how the BM25 math operates.
- [ ] Link the JavaFX `ValidationDashboard` line charts directly to the new SQLite `metrics_history.db`.
- [ ] Test the isometric engine with 500 simultaneous particles to ensure no frame-drops below 60fps.

## 🔗 BACKEND & SLM PIPELINES
- [ ] Add the Aider CLI test script into the nightly background pipeline to verify continuous code editing capability.
- [ ] Configure `model_orchestrator.py` to route strictly to `qwen2.5:0.5b` for logic and `danube` for creative writing.
- [ ] Implement a system-wide "Healing Process". If a code execution throws an `Exception`, route the stack trace back to the model that wrote it and deduct Treasury Points.

## 📂 DOCUMENTATION & VERSIONING
- [ ] Update `README.md` to perfectly describe the new EPMO and Data Syphon logic.
- [ ] Update `SYSTEM_BIBLE.md` with the new 2KB log sharding architecture.
- [ ] Timestamp EVERY markdown file in `SSD_SANDBOX/`.
- [ ] Generate a final `CHANGELOG.md` entry detailing the Phase 24 deployment.