# VIPER BOOT RESUME STATE & ACTIVE TELEMETRY
**TIMESTAMP:** 2026-06-01T06:20:00.000Z
**PROJECT_ID:** SimsMerged-v1.4
**AGENT_ID:** Antigravity-CLI-Architect

---

## 🚀 BOOT RESUME MANDATE
Whenever an AI agent boots up in this workspace, it MUST immediately read this file to synchronize its state and memory. This ensures absolute continuity without forgetting previous steps!

### 1. Active Directories & Project Context
* **Canonical Directory:** `C:\Users\viper\Desktop\SimsMerged\` (The primary development directory for SimsMerged v1.3).
* **Backup/Telemetry Directory:** `C:\Users\viper\SimAgentCity\` (Autonomous simulated engine).
* **Skills Directory:** `C:\Users\viper\.gemini\skills\` (Decentralized DePIN / OpenClaw anchors).

### 2. Completed Milestones & State
* **Milestone 1: Resource Fencing (typing lag fixed)**
  * Deployed `C:\Users\viper\resource_fence.ps1` to fence background process memory and lower process CPU priorities to `BelowNormal`.
  * Configured `NODE_OPTIONS="--max-old-space-size=512"` to cap Node memory usage to 512MB, reserving the remaining 1024MB memory pool for OS/CLI.
  * Verified typing lag has been programmatically mitigated.
* **Milestone 2: GitHub Repository and Image Protection Plan**
  * Deployed `C:\Users\viper\Desktop\SimsMerged\GITHUB_PROTECTION_GAME_PLAN.md` banning the use of `--force` push commands.
  * Locked the `assets/` directories (e.g. `ACTUAL_METROPOLIS_CITY.png`) from any file deletion scripts.

### 3. Next Actions for Performing Agents
* **NEVER CLOSE OR TERMINATE Microsoft Edge, the WebUI, or browser processes.** The user is actively using the WebUI to play, optimize models, and locate bottlenecks. Throttling and CPU priority adjustments should be done without killing the WebUI.
* **Do NOT force push** to `https://github.com/chrisalunlloyd2-sudo/SimsMerged.git`.
* Always run `resource_fence.ps1` on startup if process priorities return to Normal.
* Keep checking `PULSE_HEARTBEAT.txt` and updating it with your Atom logs under strict triple signatures.

---
*Viper, the bridge is open. We remember where we are.*
